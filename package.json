{
  "name": "@runlevel5/netlify-plugin-ejson-env",
  "version": "0.0.4",
  "description": "Netlify plugin that would populate the environment with variables defined in EJSON secrets file",
  "main": "src/index.js",
  "dependencies": {},
  "scripts": {
    "build": "netlify build",
    "test": "jest",
    "lint": "npm run eslint && npm run prettier",
    "eslint": "eslint --ignore-path .gitignore --fix --cache --format=codeframe --max-warnings=0 \"{src,init}/**/*.js\"",
    "prettier": "prettier --ignore-path .gitignore --write --loglevel warn \"{.github,src,init}/**/*.{js,md,yml,json}\" \"*.{js,md,yml,json}\"",
    "version": "auto-changelog -p --template keepachangelog && git add CHANGELOG.md"
  },
  "keywords": [
    "netlify",
    "netlify-plugin",
    "secrets",
    "ejson"
  ],
  "author": "Trung Lê <trung.le@ruby-journal.com>",
  "license": "MIT",
  "engines": {
    "node": ">=12.18.0"
  },
  "devDependencies": {
    "auto-changelog": "^1.16.2",
    "gh-release": "^3.5.0",
    "jest": "^25.1.0",
    "@netlify/build": "^18.10.0",
    "@netlify/run-utils": "^2.0.1",
    "eslint": "^7.10.0",
    "eslint-config-prettier": "^8.0.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.1.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/runlevel5/netlify-plugin-ejson-env.git"
  },
  "bugs": {
    "url": "https://github.com/runlevel5/netlify-plugin-ejson-env/issues"
  },
  "homepage": "https://github.com/runlevel5/netlify-plugin-ejson-env#readme",
  "directories": {
    "test": "tests"
  }
}
